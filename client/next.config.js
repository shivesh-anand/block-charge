/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "nextui.org",
      "images.unsplash.com",
      "youtube.com",
      "assets.aceternity.com",
    ],
  },
};

module.exports = nextConfig;
